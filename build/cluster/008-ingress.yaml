apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server-ingress
spec:
  tls:
  - hosts:
      - $HOST
    secretName: reader-cert
  defaultBackend:
    service:
      name: server
      port:
        number: 8080
