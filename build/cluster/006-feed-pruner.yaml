apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed-pruner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feed-pruner
  template:
    metadata:
      labels:
        app: feed-pruner
    spec:
      containers:
        - name: feed-pruner
          image: $IMAGE
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: secrets
          imagePullPolicy: $IMAGE_POLICY
          command: ["/bin/sh", "-c", "./jobs/main.exe feed-pruner"]
      initContainers:
        - name: wait-for-postgres
          image: postgres:13.4
          imagePullPolicy: "IfNotPresent"
          command: ["/bin/sh", "-c", "pg_isready -h postgres -p 5432 && echo $?"]
